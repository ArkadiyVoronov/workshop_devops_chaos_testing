# Database
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_DB=appdb

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=workshop

# Resource Limits (for documentation)
# app: 1 CPU, 512MB RAM
# db: 1 CPU, 512MB RAM
# prometheus: 0.5 CPU, 256MB RAM
# grafana: 0.5 CPU, 256MB RAM
# cadvisor: 0.3 CPU, 128MB RAM
# TOTAL: ~3.3 CPU cores, ~1.7GB RAM
